<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mydemo">

    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>


    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <meta-data
            android:name="design_width"
            android:value="750" />
        <meta-data
            android:name="design_height"
            android:value="1440" />

        <activity android:name=".PrintActivity"
           >
            <!-- android:theme="@style/Theme.AppCompat.NoActionBar"-->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".OnLineAty" />
        <activity android:name=".PurchaseAty" />
        <activity
            android:name=".PickingActivity"
            android:windowSoftInputMode="stateHidden" />
        <activity android:name=".AllocationActivity" />
        <activity android:name=".FeedingActivity" />
        <activity android:name=".BinningActivity" />
        <activity android:name=".ProduceOnLineActivity" />
        <activity android:name=".ReportWorkActivity" />
        <activity android:name=".MoveActivity" />
        <activity android:name=".SalesActivity" />

        <activity android:name=".RXJavaTestAty" />
    </application>

</manifest>